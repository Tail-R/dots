#! /usr/bin/env bash

set -euo pipefail

SAVE_DIR=$HOME/Pictures/screenshots
FILE_EXT=png
DP_NAME="eDP-1" # see 'wlr-randr'

mkdir -p "$SAVE_DIR"

file_name="${SAVE_DIR}/$(date '+%Y%m%d_%H%M%S').${FILE_EXT}"

grim -o "$DP_NAME" "$file_name"

